import 'package:flutter/material.dart';

class Fill_Info extends StatelessWidget {
  const Fill_Info({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        
      ),
    );
  }
}